import authService from './auth.service'
import inputTestingService from './input-testing.service'

const services = {
  auth: authService,
  inputTesting: inputTestingService,
}

export default services
